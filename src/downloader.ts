import { EventEmitter } from "node:events";
import { sep as SEP, resolve } from "node:path";

interface SimpleChannel {

}
interface SimpleGuild {

}

export class Archiver extends EventEmitter {
    archive () {

    }
}